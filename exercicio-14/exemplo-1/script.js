const paragraph = document.querySelectorAll('p.error')[1];
const title = document.querySelector("div>h1");
const playerList = document.querySelectorAll("#jogadores> ul> li");

playerList
console.log(title)
console.log(paragraph)


playerList.forEach(play => {
    console.log(play)
})